{
    "postCreateCommand": "python -m pip install --upgrade numpy ipykernel nbstripout && python -m ipykernel install --user --name=codespace-python --display-name='Python (Codespace)' && nbstripout --install --attributes .gitattributes",

    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter"
            ],
            "settings": {
                "python.defaultInterpreterPath": "/home/codespace/.python/current/bin/python"
            }
        }
    }
}